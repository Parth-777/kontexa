package com.example.BACKEND.experiment.phase1;

/**
 * A) SCHEMA_ONLY — column names and types only.
 * B) WITH_DESCRIPTIONS — approved catalogue descriptions included.
 */
public enum Phase1CataloguePayloadMode {
    SCHEMA_ONLY,
    WITH_DESCRIPTIONS
}
