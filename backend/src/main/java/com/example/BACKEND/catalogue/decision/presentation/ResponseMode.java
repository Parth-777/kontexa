package com.example.BACKEND.catalogue.decision.presentation;

/**
 * Primary response rendering mode for analyst-grade output.
 */
public enum ResponseMode {
    KPI,
    TABLE,
    CHART,
    MIXED
}
