package com.example.BACKEND.catalogue.decision.findings;

/** Structured finding types produced by {@link StructuredFindingsEngine}. */
public enum FindingType {
    CONTRIBUTION_BREAKDOWN,
    RANKING,
    EFFICIENCY,
    TEMPORAL_PATTERN,
    COMPARATIVE,
    CORRELATION
}
